{
  "name": "ezycargo-frontend-demo",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "nx run-many -t build removeNodeModules -p login main-frame counter header footer todolist",
    "build:local": "nx run-many -t build -p login main-frame counter header footer todolist",
    "dev": "nx run-many -t dev -p login main-frame counter header footer todolist --output-style=stream --parallel=6",
    "start": "nx start web-service"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@nx/js": "17.2.8",
    "nx": "17.2.8",
    "shx": "^0.3.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
